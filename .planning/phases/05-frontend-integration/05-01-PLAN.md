---
phase: 05-frontend-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/main.py
  - src/data_fetcher.py
  - src/news_fetcher.py
  - src/sentiment/scorer.py
  - src/risk_calculator.py
autonomous: true
user_setup:
  - service: fastapi
    why: "REST API backend for frontend"
    env_vars:
      - name: FASTAPI_HOST
        source: "Default: 0.0.0.0"
      - name: FASTAPI_PORT
        source: "Default: 8000"

must_haves:
  truths:
    - "FastAPI server starts and responds to requests"
    - "GET /api/risk returns {score, volatility, sentiment, level}"
    - "GET /api/market returns OHLC data for charts"
    - "GET /api/news returns headlines with sentiment scores"
    - "GET /api/history returns risk evolution data"
  artifacts:
    - path: "api/main.py"
      provides: "FastAPI application with all endpoints"
      exports: ["GET /api/risk", "GET /api/market", "GET /api/news", "GET /api/history"]
    - path: "api/models.py"
      provides: "Pydantic response models"
      exports: ["RiskResponse", "MarketResponse", "NewsResponse"]
  key_links:
    - from: "api/main.py"
      to: "src/data_fetcher.py"
      via: "import and function calls"
      pattern: "from src.data_fetcher import"
    - from: "api/main.py"
      to: "src/risk_calculator.py"
      via: "import and function calls"
      pattern: "from src.risk_calculator import"
    - from: "api/main.py"
      to: "src/news_fetcher.py"
      via: "import and function calls"
      pattern: "from src.news_fetcher import"
---

<objective>
Create FastAPI backend to expose market risk data as REST API endpoints

Purpose: Replace Streamlit with FastAPI so React frontend can fetch real data instead of using mock data
Output: Running FastAPI server with 4 endpoints: /api/risk, /api/market, /api/news, /api/history
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@app.py
@src/risk_calculator.py

# Reference existing Python modules:
# - src/data_fetcher.py: fetch_market_data(), fetch_historical_data()
# - src/news_fetcher.py: fetch_all_news()
# - src/sentiment/scorer.py: get_sentiment_scores()
# - src/risk_calculator.py: get_all_metrics(), get_risk_level()
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FastAPI main application with response models</name>
  <files>api/models.py, api/main.py</files>
  <action>
    Create api/models.py with Pydantic response models:
    - RiskResponse: score (float), volatility (float), sentiment (float), level (str), timestamp
    - MarketDataPoint: time, price, volume, open, high, low, close
    - MarketResponse: data (list of MarketDataPoint), symbol, time_range
    - NewsItem: title, source, sentiment, url, time
    - NewsResponse: articles (list of NewsItem), total_count, timestamp
    - HistoryPoint: time, risk, volatility, sentiment
    - HistoryResponse: data (list of HistoryPoint), period_hours

    Create api/main.py with FastAPI app:
    - Import all required functions from src modules
    - Add CORS middleware for frontend origin
    - Create GET /api/risk endpoint calling data fetcher + risk calculator
    - Create GET /api/market endpoint returning OHLC data
    - Create GET /api/news endpoint returning headlines with sentiment
    - Create GET /api/history endpoint returning 24h risk evolution
    - Use @app.get() decorators with response_model
    - Add caching via simple in-memory dict with TTL
  </action>
  <verify>
    1. Run: cd E:\Projects\vix sentinel && python -c "from api.main import app; print('FastAPI imports OK')"
    2. Start server: uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
    3. Test: curl http://localhost:8000/api/risk returns JSON with risk score
    4. Test: curl http://localhost:8000/api/market returns OHLC data
    5. Test: curl http://localhost:8000/api/news returns news with sentiment
  </verify>
  <done>
    FastAPI server starts on port 8000, all 4 endpoints return valid JSON responses
    with proper risk_score, market data, news with sentiment, and history data
  </done>
</task>

<task type="auto">
  <name>Task 2: Add CORS and integrate existing Python logic</name>
  <files>api/main.py</files>
  <action>
    Update api/main.py to:
    - Add CORS middleware: app.add_middleware with allow_origins=["http://localhost:5173", "http://localhost:3000"], allow_credentials=True
    - Wire /api/risk to call: load_market_data() -> load_news_and_sentiment() -> calculate_risk_metrics() -> return avg risk
    - Wire /api/market to return SPY historical data as OHLC format
    - Wire /api/news to return fetch_all_news() with sentiment scores from get_sentiment_scores()
    - Wire /api/history to generate 24h of mock history data (for initial implementation)
    - Add error handling: try/except around data fetching, return {"error": str(e)} on failure
  </action>
  <verify>
    1. curl -H "Origin: http://localhost:5173" http://localhost:8000/api/risk includes CORS headers
    2. /api/risk returns: {"score": float, "volatility": float, "sentiment": float, "level": "LOW|MEDIUM|HIGH"}
    3. /api/market returns array of {time, price, volume} objects
    4. /api/news returns array of {title, source, sentiment} objects
  </verify>
  <done>
    All endpoints return properly formatted JSON with CORS headers enabled
    for frontend localhost origins
  </done>
</task>

<task type="auto">
  <name>Task 3: Add caching and startup configuration</name>
  <files>api/main.py, requirements.txt</files>
  <action>
    Update api/main.py to:
    - Add simple in-memory cache with 5-minute TTL (15 min * 60 sec = 900s matches Streamlit)
    - Cache structure: {"risk": {"data": ..., "timestamp": ...}, "market": {...}, "news": {...}}
    - Check cache before fetching data, return cached if fresh
    - Add startup event: @app.on_event("startup") to warm cache
    - Add /api/refresh endpoint to force cache refresh

    Update requirements.txt to add:
    - fastapi>=0.100.0
    - uvicorn[standard]>=0.23.0
    - python-multipart>=0.0.6
  </action>
  <verify>
    1. Check requirements.txt has fastapi and uvicorn
    2. First /api/risk call fetches fresh data
    3. Second /api/risk call within 5 minutes returns cached data
    4. /api/refresh clears cache and returns fresh data
  </verify>
  <done>
    API has 5-minute caching, startup cache warming works,
    and /api/refresh endpoint forces data refresh
  </done>
</task>

</tasks>

<verification>
- FastAPI starts without errors on `uvicorn api.main:app --reload`
- All 4 endpoints return valid JSON with correct structure
- CORS enabled for localhost:5173 and localhost:3000
- Cache reduces redundant data fetching on repeated calls
</verification>

<success_criteria>
1. FastAPI server runs on port 8000
2. GET /api/risk returns {score, volatility, sentiment, level, timestamp}
3. GET /api/market returns OHLC data array
4. GET /api/news returns articles with sentiment scores
5. GET /api/history returns 24h risk evolution data
6. CORS configured for frontend origins
7. 5-minute cache implemented
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-integration/05-01-SUMMARY.md`
</output>
