---
phase: 05-frontend-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - "Market Risk Monitoring Dashboard/src/app/App.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/RiskGauge.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/MarketChart.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/SentimentChart.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/NewsFeed.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/VolatilityHeatmap.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/RiskEvolutionChart.tsx"
autonomous: false
user_setup: []

must_haves:
  truths:
    - "Skeleton loading states show while data fetches"
    - "Responsive layout works on mobile/tablet/desktop"
    - "Auto-refresh shows visual indicator during refresh"
    - "User can verify complete integration works"
  artifacts:
    - path: "Market Risk Monitoring Dashboard/src/app/components/ui/skeleton.tsx"
      provides: "Already exists - use for loading states"
  key_links:
    - from: "App.tsx"
      to: "skeleton.tsx"
      via: "conditional rendering"
      pattern: "loading.*Skeleton"
---

<objective>
Add UI enhancements: skeleton loading states, responsive layout, auto-refresh indicator

Purpose: Improve UX with loading states and ensure responsive design works across devices
Output: Complete frontend with skeletons, responsive layout, and visual refresh indicator
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
# Reference skeleton component:
@Market Risk Monitoring Dashboard/src/app/components/ui/skeleton.tsx

# Existing App.tsx (from Plan 05-02):
# - Has loading state
# - Fetches from API
# - Has auto-refresh interval
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add skeleton loading states to all components</name>
  <files>
    Market Risk Monitoring Dashboard/src/app/App.tsx
    Market Risk Monitoring Dashboard/src/app/components/RiskGauge.tsx
    Market Risk Monitoring Dashboard/src/app/components/MarketChart.tsx
    Market Risk Monitoring Dashboard/src/app/components/SentimentChart.tsx
    Market Risk Monitoring Dashboard/src/app/components/NewsFeed.tsx
    Market Risk Monitoring Dashboard/src/app/components/VolatilityHeatmap.tsx
    Market Risk Monitoring Dashboard/src/app/components/RiskEvolutionChart.tsx
  </files>
  <action>
    Update App.tsx and components to show skeleton during loading:
    
    App.tsx:
    - Add loading state for each data type: const [riskLoading, setRiskLoading] = useState(true)
    - Pass loading prop to each component
    - When loading, show skeleton placeholders instead of components
    
    RiskGauge.tsx:
    - Accept loading prop: loading?: boolean
    - When loading: show Skeleton in circular shape (w-64 h-32) with "Loading..." text
    
    MarketChart.tsx:
    - Accept loading prop
    - When loading: show Skeleton with h-80, w-full, rounded-lg
    
    SentimentChart.tsx:
    - Accept loading prop  
    - When loading: show Skeleton chart placeholder
    
    NewsFeed.tsx:
    - Accept loading prop
    - When loading: show 5 Skeleton items (h-16 each) for news placeholders
    
    VolatilityHeatmap.tsx:
    - Accept loading prop
    - When loading: show grid of Skeleton cells
    
    RiskEvolutionChart.tsx:
    - Accept loading prop
    - When loading: show Skeleton line chart placeholder
  </action>
  <verify>
    1. Initial page load shows skeleton placeholders
    2. After data arrives, skeletons replaced with real data
    3. No layout shift when loading completes
  </verify>
  <done>
    All components show appropriate skeleton loading states
    while fetching data from API
  </done>
</task>

<task type="auto">
  <name>Task 2: Add visual refresh indicator for auto-refresh</name>
  <files>Market Risk Monitoring Dashboard/src/app/App.tsx</files>
  <action>
    Update App.tsx auto-refresh to show visual indicator:
    - Keep existing isRefreshing state
    - Show spinner/loading in header during refresh:
      - Add rotating icon in header when isRefreshing = true
      - Show "Refreshing..." text next to last update time
    - Add small fade animation during refresh
    - Keep 15-minute interval (15 * 60 * 1000 ms)
    - Show toast/notification: "Data refreshed" after each refresh
  </action>
  <verify>
    1. Every 15 minutes, header shows refreshing indicator
    2. User can see when data is being updated
    3. "Refreshing..." text appears during update
  </verify>
  <done>
    Auto-refresh shows visual indicator in header during each refresh cycle
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 5 integration: FastAPI backend + React frontend with skeletons</what-built>
  <how-to-verify>
    1. Start FastAPI backend: cd E:\Projects\vix sentinel && uvicorn api.main:app --reload --port 8000
    2. Start React frontend: cd "Market Risk Monitoring Dashboard" && npm run dev
    3. Open http://localhost:5173 in browser
    4. Verify:
       - Skeleton loading states show initially
       - Risk score displays (0-100 with LOW/MEDIUM/HIGH color)
       - Market chart shows data
       - News shows with sentiment indicators (green/red)
       - 24h risk evolution chart displays
       - Auto-refresh works (check after 15 min or click Refresh)
    5. Test responsive: resize browser to mobile/tablet widths
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Skeleton components show during initial load
- All charts render with real API data
- Responsive layout works on mobile/tablet/desktop
- Auto-refresh shows indicator during refresh
- No console errors on load
</verification>

<success_criteria>
1. Skeleton loading states appear on initial load
2. RiskGauge shows score with correct color (LOW=green, MEDIUM=yellow, HIGH=red)
3. MarketChart renders OHLC/candlestick data
4. NewsFeed shows articles with sentiment indicators
5. RiskEvolutionChart shows 24h history
6. Responsive: layout adapts at lg breakpoint (1024px)
7. Auto-refresh indicator visible during refresh
8. Human verification confirms all features work
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-integration/05-03-SUMMARY.md`
</output>
