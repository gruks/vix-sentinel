---
phase: 05-frontend-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - "Market Risk Monitoring Dashboard/src/app/App.tsx"
  - "Market Risk Monitoring Dashboard/src/app/services/api.ts"
  - "Market Risk Monitoring Dashboard/src/app/components/RiskGauge.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/MarketChart.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/SentimentChart.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/NewsFeed.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/VolatilityHeatmap.tsx"
  - "Market Risk Monitoring Dashboard/src/app/components/RiskEvolutionChart.tsx"
autonomous: true
user_setup:
  - service: frontend
    why: "React frontend needs to fetch from FastAPI"
    env_vars:
      - name: VITE_API_URL
        source: "Default: http://localhost:8000"

must_haves:
  truths:
    - "Frontend fetches risk data from FastAPI /api/risk"
    - "Frontend fetches market data from /api/market"
    - "Frontend fetches news from /api/news"
    - "Frontend fetches history from /api/history"
    - "Components render real data instead of mock data"
  artifacts:
    - path: "Market Risk Monitoring Dashboard/src/app/services/api.ts"
      provides: "API service layer with fetch functions"
      exports: ["fetchRisk", "fetchMarket", "fetchNews", "fetchHistory"]
    - path: "Market Risk Monitoring Dashboard/src/app/App.tsx"
      provides: "Main app with API integration"
      imports: ["api.ts functions"]
  key_links:
    - from: "src/app/services/api.ts"
      to: "http://localhost:8000/api/*"
      via: "fetch calls"
      pattern: "fetch.*http://localhost:8000"
    - from: "src/app/App.tsx"
      to: "src/app/services/api.ts"
      via: "useEffect + useState"
      pattern: "useEffect.*fetchRisk"
---

<objective>
Integrate React frontend with FastAPI backend endpoints

Purpose: Replace mock data generators in App.tsx with real API calls to FastAPI
Output: Frontend displays live data from Python backend
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
# Reference existing React components:
@Market Risk Monitoring Dashboard/src/app/App.tsx
@Market Risk Monitoring Dashboard/src/app/components/RiskGauge.tsx
@Market Risk Monitoring Dashboard/src/app/components/MarketChart.tsx
@Market Risk Monitoring Dashboard/src/app/components/SentimentChart.tsx
@Market Risk Monitoring Dashboard/src/app/components/NewsFeed.tsx

# API endpoints from Plan 05-01:
# - GET /api/risk: {score, volatility, sentiment, level, timestamp}
# - GET /api/market: {data: [{time, price, volume, open, high, low, close}], symbol, time_range}
# - GET /api/news: {articles: [{title, source, sentiment, url, time}], total_count}
# - GET /api/history: {data: [{time, risk, volatility, sentiment}], period_hours}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API service layer</name>
  <files>Market Risk Monitoring Dashboard/src/app/services/api.ts</files>
  <action>
    Create Market Risk Monitoring Dashboard/src/app/services/api.ts:
    - Define TypeScript interfaces matching FastAPI response models:
      - RiskResponse: { score: number, volatility: number, sentiment: number, level: string, timestamp: string }
      - MarketDataPoint: { time: string, price: number, volume: number, open?: number, high?: number, low?: number, close?: number }
      - MarketResponse: { data: MarketDataPoint[], symbol: string, time_range: string }
      - NewsItem: { title: string, source: string, sentiment: number, url: string, time: string }
      - NewsResponse: { articles: NewsItem[], total_count: number }
      - HistoryPoint: { time: string, risk: number, volatility: number, sentiment: number }
      - HistoryResponse: { data: HistoryPoint[], period_hours: number }
    - Create const API_BASE = import.meta.env.VITE_API_URL || "http://localhost:8000"
    - Implement fetchRisk(): Promise<RiskResponse>
    - Implement fetchMarket(symbol?: string, timeRange?: string): Promise<MarketResponse>
    - Implement fetchNews(): Promise<NewsResponse>
    - Implement fetchHistory(periodHours?: number): Promise<HistoryResponse>
    - Add error handling: try/catch, return null on error, log to console
  </action>
  <verify>
    1. Run: cd "Market Risk Monitoring Dashboard" && npm run dev (in background)
    2. Check browser console for CORS errors when calling API
    3. Test fetch functions return properly typed data
    4. Verify error handling works when API is down
  </verify>
  <done>
    api.ts exports fetchRisk, fetchMarket, fetchNews, fetchHistory functions
    with proper TypeScript types matching FastAPI responses
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate API into App.tsx</name>
  <files>Market Risk Monitoring Dashboard/src/app/App.tsx</files>
  <action>
    Update App.tsx:
    - Import { fetchRisk, fetchMarket, fetchNews, fetchHistory } from "./services/api"
    - Replace mock data useState with loading state: const [loading, setLoading] = useState(true)
    - Replace mock data generators with useEffect that calls API:
      - useEffect(() => { fetchRisk().then(data => setRiskData(data)) }, [])
      - Similar for market, news, history
    - Add loading state check: if (loading) return <div>Loading...</div>
    - Update risk score calculation to use API data instead of mock:
      - const riskScore = riskData?.score ?? calculatedRisk
      - const riskLevel = riskData?.level ?? "MEDIUM"
    - Pass API data to child components instead of mock data
    - Keep auto-refresh: setInterval that calls all fetch functions every 15 minutes
  </action>
  <verify>
    1. Frontend shows loading state initially
    2. After API response, dashboard shows real risk score from Python backend
    3. Market chart displays data from /api/market
    4. News shows articles from /api/news
    5. Auto-refresh updates data every 15 minutes
  </verify>
  <done>
    App.tsx fetches real data from FastAPI, displays loading state during fetch,
    and auto-refreshes every 15 minutes
  </done>
</task>

<task type="auto">
  <name>Task 3: Update components to handle real data</name>
  <files>
    Market Risk Monitoring Dashboard/src/app/components/RiskGauge.tsx
    Market Risk Monitoring Dashboard/src/app/components/MarketChart.tsx
    Market Risk Monitoring Dashboard/src/app/components/SentimentChart.tsx
    Market Risk Monitoring Dashboard/src/app/components/NewsFeed.tsx
    Market Risk Monitoring Dashboard/src/app/components/VolatilityHeatmap.tsx
    Market Risk Monitoring Dashboard/src/app/components/RiskEvolutionChart.tsx
  </files>
  <action>
    Update each component to handle real API data format:
    
    RiskGauge.tsx:
    - Accept riskScore as number directly (already does)
    - Accept level prop: "LOW" | "MEDIUM" | "HIGH"
    - Use level prop for color instead of calculating
    
    MarketChart.tsx:
    - Accept data prop matching MarketDataPoint[] from API
    - Handle both mock format and API format (check for 'price' vs 'close')
    - Support both array and object price formats
    
    SentimentChart.tsx:
    - Accept data from API history endpoint or sentiment from risk endpoint
    - Calculate averageSentiment from API data
    
    NewsFeed.tsx:
    - Accept articles matching NewsItem[] from API
    - Handle sentiment values from -1 to 1 range
    - Show sentiment indicator (positive=green, negative=red)
    
    VolatilityHeatmap.tsx:
    - Accept volatility data from API or use risk endpoint data
    - Handle empty/undefined data gracefully
    
    RiskEvolutionChart.tsx:
    - Accept data from /api/history endpoint
    - Map {time, risk} to chart format
  </action>
  <verify>
    1. RiskGauge shows correct color for LOW/MEDIUM/HIGH from API
    2. MarketChart renders OHLC data from API
    3. NewsFeed shows sentiment indicators (green/red) for each article
    4. RiskEvolutionChart shows 24h history from API
    5. All components handle loading/empty states
  </verify>
  <done>
    All components properly render real API data and handle
    various data formats gracefully
  </done>
</task>

</tasks>

<verification>
- Frontend builds without TypeScript errors
- All API calls succeed and return data
- Components display data from Python backend
- Auto-refresh works every 15 minutes
</verification>

<success_criteria>
1. Frontend fetches from http://localhost:8000 API endpoints
2. RiskGauge displays risk score from API (0-100 with LOW/MEDIUM/HIGH)
3. MarketChart renders market data from API
4. NewsFeed shows news with sentiment indicators
5. RiskEvolutionChart displays 24h history
6. Loading state shows while fetching
7. Auto-refresh updates every 15 minutes
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-integration/05-02-SUMMARY.md`
</output>
