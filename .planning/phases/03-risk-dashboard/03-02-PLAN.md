---
phase: 03-risk-dashboard
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/charts.py]
autonomous: true

must_haves:
  truths:
    - "User sees 3 Plotly gauges: volatility, sentiment, risk"
    - "User sees risk alert banner with color based on level"
    - "User sees 7-day SPY candlestick chart"
    - "User sees 7-day sentiment trend line chart"
    - "User sees volatility heatmap (tickers x days)"
  artifacts:
    - path: "src/charts.py"
      provides: "Chart creation functions: create_gauge, create_candlestick, create_sentiment_line, create_volatility_heatmap, create_risk_evolution"
      min_lines: 150
  key_links:
    - from: "src/charts.py"
      to: "src/risk_calculator.py"
      via: "import get_risk_level"
      pattern: "from src.risk_calculator import"
    - from: "src/charts.py"
      to: "src/data_fetcher.py"
      via: "import fetch_historical_data"
      pattern: "from src.data_fetcher import"
---

<objective>
Build the charts module with all Plotly visualizations for the dashboard.

Purpose: Create reusable chart functions for gauges, candlesticks, line charts, and heatmaps.
Output: src/charts.py with chart creation functions
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-risk-dashboard/03-01-PLAN.md
@.planning/phases/03-risk-dashboard/03-RESEARCH.md

# Must reference risk_calculator for level colors:
@src/risk_calculator.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Plotly gauge functions</name>
  <files>src/charts.py</files>
  <action>
Create src/charts.py with:

1. create_gauge(value, title, mode="gauge+number") -> go.Figure:
   - Use plotly.graph_objects go.Indicator
   - Set axis range [0, 100]
   - Add color steps:
     - GREEN (#4CAF50): 0-40 (LOW)
     - YELLOW (#FFC107): 40-75 (MEDIUM)  
     - RED (#F44336): 75-100 (HIGH)
   - Add threshold marker at current value
   - Support use_container_width via layout settings

2. create_three_gauges(volatility, sentiment, risk_score) -> list:
   - Returns 3 gauge figures in a list
   - volatility gauge: shows volatility Z-score (can exceed 100, cap display)
   - sentiment gauge: shows sentiment 0-100 (normalized from 0-1)
   - risk gauge: shows risk score 0-100 with threshold colors

Reference research: Use go.Indicator with steps for threshold coloring
  </action>
  <verify>python -c "
import sys
sys.path.insert(0, '.')
from src.charts import create_gauge, create_three_gauges
# Test single gauge
g = create_gauge(65, 'Test Risk')
assert g is not None, 'Should return Figure'
# Test three gauges
gauges = create_three_gauges(0.5, 0.6, 55)
assert len(gauges) == 3, 'Should return 3 gauges'
print('Tests passed')
"</verify>
  <done>create_gauge returns Plotly figure, create_three_gauges returns 3 gauge figures</done>
</task>

<task type="auto">
  <name>Task 2: Create candlestick and sentiment trend charts</name>
  <files>src/charts.py</files>
  <action>
Add to src/charts.py:

1. create_candlestick(df, title="SPY 7-Day Market Trend") -> go.Figure:
   - Input: DataFrame with OHLC columns (Open, High, Low, Close)
   - Use go.Candlestick with x=df.index
   - Set increasing_line_color='green', decreasing_line_color='red'
   - Hide rangeslider (xaxis_rangeslider_visible=False)
   - Set height ~400px for readability

2. create_sentiment_line(dates, sentiment_values, title="Sentiment Trend (7-Day Avg)") -> go.Figure:
   - Input: list of dates, list of sentiment scores (0-1)
   - Use go.Scatter with mode='lines+markers'
   - Add reference line at 0.5 (neutral sentiment)
   - Color line based on average: green if >0.5, red if <0.5
   - Set y-axis range [0, 1]

Reference research: go.Candlestick for market data, go.Scatter for trends
  </action>
<verify>python -c "
import sys, pandas as pd
sys.path.insert(0, '.')
from src.charts import create_candlestick, create_sentiment_line
# Test candlestick with mock data
df = pd.DataFrame({
    'Open': [100, 101, 102], 
    'High': [105, 106, 107],
    'Low': [98, 99, 100], 
    'Close': [104, 105, 106]
})
candle = create_candlestick(df)
assert candle is not None
# Test sentiment line
dates = ['2024-01-01', '2024-01-02', '2024-01-03']
sentiments = [0.6, 0.4, 0.7]
line = create_sentiment_line(dates, sentiments)
assert line is not None
print('Tests passed')
"</verify>
  <done>create_candlestick and create_sentiment_line return Plotly figures</done>
</task>

<task type="auto">
  <name>Task 3: Create volatility heatmap and risk evolution charts</name>
  <files>src/charts.py</files>
  <action>
Add to src/charts.py:

1. create_volatility_heatmap(volatility_data, tickers, dates, title="Volatility Heatmap") -> go.Figure:
   - Input: volatility_data = {ticker: [vol_day1, vol_day2, ...]}
   - Use go.Heatmap with z=[volatility_data[t] for t in tickers]
   - Set colorscale='RdYlGn_r' (red=high vol, green=low)
   - Add x=dates, y=tickers labels
   - Add colorbar with label "Volatility"

2. create_risk_evolution(timestamps, risk_scores, title="Risk Score Evolution (24h)") -> go.Figure:
   - Input: timestamps (list of datetime), risk_scores (list of 0-100 values)
   - Use go.Scatter with mode='lines'
   - Add horizontal lines at 40 and 75 for threshold markers
   - Color line segments: green below 40, yellow 40-75, red above 75
   - Set y-axis range [0, 100], x-axis as datetime

3. create_risk_banner(risk_level, alert_type) -> dict:
   - Returns dict with message and icon for Streamlit alert
   - Messages:
     - LOW: "Market conditions appear stable. Risk levels are within normal range."
     - MEDIUM: "Elevated volatility detected. Monitor positions closely."
     - HIGH: "WARNING: High risk conditions. Consider defensive positioning."
   - alert_type maps to Streamlit: success/warning/error

Reference research: go.Heatmap for volatility, go.Scatter for time series
  </action>
<verify>python -c "
import sys
sys.path.insert(0, '.')
from src.charts import create_volatility_heatmap, create_risk_evolution, create_risk_banner
# Test heatmap
vol_data = {'SPY': [0.15, 0.18], 'VIX': [0.25, 0.30]}
heatmap = create_volatility_heatmap(vol_data, ['SPY', 'VIX'], ['Day1', 'Day2'])
assert heatmap is not None
# Test evolution
from datetime import datetime
ts = [datetime(2024, 1, 1, i) for i in range(24)]
scores = [30 + i*2 for i in range(24)]
evolution = create_risk_evolution(ts, scores)
assert evolution is not None
# Test banner
banner = create_risk_banner('HIGH', 'error')
assert 'message' in banner
print('Tests passed')
"</verify>
  <done>Heatmap, risk evolution, and banner functions return proper outputs</done>
</task>

</tasks>

<verification>
1. Import test: `from src.charts import create_gauge, create_candlestick, create_sentiment_line, create_volatility_heatmap, create_risk_evolution`
2. Verify gauge: has color steps matching LOW/MEDIUM/HIGH thresholds
3. Verify candlestick: renders OHLC data with green/red colors
4. Verify heatmap: shows tickers x days with RdYlGn_r colorscale
5. Verify risk evolution: shows 24h history with threshold lines
</verification>

<success_criteria>
- [ ] create_gauge with threshold color steps works
- [ ] create_three_gauges returns 3 Plotly figures
- [ ] create_candlestick renders OHLC data
- [ ] create_sentiment_line shows 7-day trend
- [ ] create_volatility_heatmap shows ticker x day grid
- [ ] create_risk_evolution shows 24h history with thresholds
- [ ] create_risk_banner returns correct message + icon
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-dashboard/03-02-SUMMARY.md`
</output>
