---
phase: 03-risk-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/risk_calculator.py]
autonomous: true

must_haves:
  truths:
    - "Volatility Z-score calculated against historical mean"
    - "Combined risk score computed using locked formula: 0.6*vol_z + 0.4*(1-sentiment)"
    - "Risk thresholds applied: LOW (<40), MEDIUM (40-75), HIGH (>75)"
    - "Alert colors determined: GREEN, YELLOW, RED"
  artifacts:
    - path: "src/risk_calculator.py"
      provides: "Risk calculation functions: calculate_volatility_zscore, calculate_risk_score, get_risk_level"
      min_lines: 100
  key_links:
    - from: "src/risk_calculator.py"
      to: "src/data_fetcher.py"
      via: "import calculate_volatility"
      pattern: "from src.data_fetcher import"
    - from: "src/risk_calculator.py"
      to: "src/sentiment"
      via: "import calculate_average_sentiment"
      pattern: "from src.sentiment.scorer import"
---

<objective>
Build the core risk calculation module that combines volatility Z-scores with sentiment data to produce risk scores.

Purpose: This is the mathematical foundation for the dashboard - implements the LOCKED risk formula.
Output: src/risk_calculator.py with pure functions for risk calculation
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-pipeline/01-01-SUMMARY.md
@.planning/phases/03-risk-dashboard/03-RESEARCH.md

# Existing source files:
@src/data_fetcher.py
@src/news_fetcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement volatility Z-score calculation</name>
  <files>src/risk_calculator.py</files>
  <action>
Create src/risk_calculator.py with function calculate_volatility_zscore(current_vol, historical_vols):
- Accept current_vol (float) and historical_vols (list of floats)
- Calculate mean and std of historical_vols using numpy
- Return Z-score: (current_vol - mean) / std
- Handle edge cases: if std==0 or len(historical_vols)<2, return 0.0
- Add docstring explaining Z-score normalization

Reference: research shows Z-score requires minimum 20 days for reliability, will use available data
  </action>
  <verify>python -c "
from src.risk_calculator import calculate_volatility_zscore
# Test with sample data
current = 0.20
history = [0.15, 0.18, 0.12, 0.16, 0.14, 0.17, 0.13]
zscore = calculate_volatility_zscore(current, history)
print(f'Z-score: {zscore:.2f}')
assert isinstance(zscore, float), 'Should return float'
print('Test passed')
"</verify>
  <done>calculate_volatility_zscore returns normalized Z-score float</done>
</task>

<task type="auto">
  <name>Task 2: Implement combined risk score calculation</name>
  <files>src/risk_calculator.py</files>
  <action>
Add to src/risk_calculator.py:

1. calculate_risk_score(vol_zscore, sentiment_score):
   - Per LOCKED formula: risk = 0.6 * vol_z + 0.4 * (1 - sentiment)
   - sentiment_score expected in 0-1 scale (0=negative, 1=positive)
   - (1 - sentiment) inverts: negative sentiment = higher risk
   - Returns float 0-100 (multiply result by 100)

2. normalize_sentiment(sentiment_value):
   - Accept sentiment in -1 to +1 or 0-1 range
   - Normalize to 0-1 scale for risk formula
   - If input is -1 to +1: return (value + 1) / 2
   - If input is 0-1: return as-is

LOCKED formula must be implemented exactly: 0.6*vol_z + 0.4*(1-sentiment)
  </action>
  <verify>python -c "
from src.risk_calculator import calculate_risk_score, normalize_sentiment
# Test normalize
assert normalize_sentiment(-1.0) == 0.0, 'Negative should map to 0'
assert normalize_sentiment(1.0) == 1.0, 'Positive should map to 1'
assert normalize_sentiment(0.0) == 0.5, 'Neutral should map to 0.5'
# Test risk calculation
risk = calculate_risk_score(1.0, 0.0)  # high vol, negative sentiment
print(f'Risk score: {risk:.1f}')
assert 0 <= risk <= 100, 'Risk should be 0-100'
print('Tests passed')
"</verify>
  <done>calculate_risk_score returns 0-100 score using LOCKED formula</done>
</task>

<task type="auto">
  <name>Task 3: Implement risk level and alert color determination</name>
  <files>src/risk_calculator.py</files>
  <action>
Add to src/risk_calculator.py:

1. get_risk_level(risk_score) -> tuple:
   - Input: risk_score (0-100)
   - Per LOCKED thresholds:
     - < 40: returns ("LOW", "GREEN", "success")
     - 40-75: returns ("MEDIUM", "YELLOW", "warning") 
     - > 75: returns ("HIGH", "RED", "error")
   - Handle edge case: score exactly 40 or 75 returns MEDIUM

2. get_all_metrics(volatility, historical_vols, sentiment_score) -> dict:
   - Combines all calculations: Z-score, normalized sentiment, risk score, level
   - Returns comprehensive dict with all values for dashboard display
   - Returns: {vol_zscore, normalized_sentiment, risk_score, level, color, alert_type}

LOCKED thresholds must be implemented exactly: LOW(<40), MEDIUM(40-75), HIGH(>75)
  </action>
  <verify>python -c "
from src.risk_calculator import get_risk_level, get_all_metrics
# Test thresholds
assert get_risk_level(30) == ('LOW', 'GREEN', 'success')
assert get_risk_level(40) == ('MEDIUM', 'YELLOW', 'warning')
assert get_risk_level(75) == ('MEDIUM', 'YELLOW', 'warning')
assert get_risk_level(80) == ('HIGH', 'RED', 'error')
# Test combined
metrics = get_all_metrics(0.20, [0.15]*30, 0.3)
print(f'Full metrics: {metrics}')
assert 'risk_score' in metrics
assert 'level' in metrics
print('Tests passed')
"</verify>
  <done>get_risk_level returns correct thresholds, get_all_metrics returns full dashboard data</done>
</task>

</tasks>

<verification>
1. Import test: `from src.risk_calculator import calculate_volatility_zscore, calculate_risk_score, get_risk_level`
2. Verify Z-score: returns float, handles edge cases (std=0, empty history)
3. Verify risk formula: 0.6*vol_z + 0.4*(1-sentiment) produces 0-100
4. Verify thresholds: LOW(<40), MEDIUM(40-75), HIGH(>75) with correct colors
</verification>

<success_criteria>
- [ ] calculate_volatility_zscore returns normalized Z-score
- [ ] calculate_risk_score implements LOCKED formula exactly
- [ ] get_risk_level returns correct threshold + color + alert_type
- [ ] get_all_metrics combines all calculations into single dict
- [ ] All functions have proper docstrings and error handling
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-dashboard/03-01-SUMMARY.md`
</output>
