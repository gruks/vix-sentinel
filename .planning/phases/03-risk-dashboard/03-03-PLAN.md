---
phase: 03-risk-dashboard
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: [app.py]
autonomous: false

must_haves:
  truths:
    - "Sidebar shows ticker multiselect with add/remove capability"
    - "Sidebar shows time range selector (1d/7d/30d)"
    - "Dashboard auto-refreshes every 15 minutes"
    - "Manual refresh button clears cache and reloads data"
    - "Data is cached with st.cache_data TTL=900s"
    - "Per-ticker details table displays in dashboard"
  artifacts:
    - path: "app.py"
      provides: "Complete dashboard with sidebar config, data loading, risk calculation, all charts"
      min_lines: 200
  key_links:
    - from: "app.py"
      to: "src/risk_calculator.py"
      via: "import get_all_metrics"
      pattern: "from src.risk_calculator import"
    - from: "app.py"
      to: "src/charts.py"
      via: "import create_three_gauges, create_candlestick, etc"
      pattern: "from src.charts import"
    - from: "app.py"
      to: "src/data_fetcher.py"
      via: "import fetch_market_data, fetch_historical_data"
      pattern: "from src.data_fetcher import"
    - from: "app.py"
      to: "src/sentiment"
      via: "import get_sentiment_scores"
      pattern: "from src.sentiment"
---

<objective>
Integrate all components into the main Streamlit dashboard with sidebar configuration.

Purpose: Wire together risk calculator, charts, data fetchers, and sentiment into complete UI.
Output: Updated app.py with full dashboard
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-risk-dashboard/03-01-PLAN.md
@.planning/phases/03-risk-dashboard/03-02-PLAN.md
@.planning/phases/03-risk-dashboard/03-RESEARCH.md

# Must integrate all modules:
@src/risk_calculator.py
@src/charts.py
@src/data_fetcher.py
@src/news_fetcher.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add sidebar configuration to app.py</name>
  <files>app.py</files>
  <action>
Update app.py with sidebar configuration (replace existing placeholder sidebar):

1. Add streamlit-autorefresh import at top:
   from streamlit_autorefresh import st_autorefresh

2. Add auto-refresh setup after st.set_page_config:
   st_autorefresh(interval=15*60*1000, limit=None, key="dashboard_refresh")

3. Sidebar Configuration section:
   st.sidebar.title("Configuration")
   
   - Ticker multiselect:
     default_tickers = ["SPY", "VIX", "QQQ", "IWM"]
     available = ["SPY", "VIX", "QQQ", "IWM", "AAPL", "TSLA", "MSFT", "NVDA", "AMD", "META"]
     selected_tickers = st.sidebar.multiselect("Select Tickers", available, default_tickers)
   
   - Time range radio:
     time_range = st.sidebar.radio("Time Range", ["1d", "7d", "30d"], horizontal=True)
   
   - Manual refresh button:
     if st.sidebar.button("Refresh Now"):
         st.cache_data.clear()
         st.rerun()
   
   - Display selected config summary

Reference: CFG-01 to CFG-04, streamlit-autorefresh for CFG-03
  </action>
<verify>grep -q "st_autorefresh" app.py && grep -q "multiselect" app.py && echo "Sidebar config added"</verify>
  <done>app.py has sidebar with ticker multiselect, time range, auto-refresh, manual refresh</done>
</task>

<task type="auto">
  <name>Task 2: Add data loading functions with caching</name>
  <files>app.py</files>
<action>
Add to app.py (after imports, before main UI):

1. Add cached data loading functions:

@st.cache_data(ttl=900, show_spinner=False)
def load_market_data(tickers, time_range):
    from src.data_fetcher import fetch_market_data, fetch_historical_data
    current = fetch_market_data(tickers)
    historical = {}
    for t in tickers:
        historical[t] = fetch_historical_data(t, time_range)
    return current, historical

@st.cache_data(ttl=900, show_spinner=False)
def load_news_and_sentiment(tickers):
    from src.news_fetcher import fetch_all_news
    from src.sentiment import get_sentiment_scores
    news = fetch_all_news(tickers)
    sentiment = get_sentiment_scores(news)
    return news, sentiment

@st.cache_data(ttl=900, show_spinner=False)
def calculate_risk_metrics(market_data, sentiment_data, tickers):
    from src.risk_calculator import get_all_metrics, calculate_volatility_zscore
    # Calculate metrics for each ticker
    results = {}
    for ticker in tickers:
        vol = market_data.get(ticker, {}).get('volatility', 0.15)
        hist_vols = [vol * 0.8, vol * 0.9, vol, vol * 1.1, vol * 1.2]  # Mock history
        vol_z = calculate_volatility_zscore(vol, hist_vols)
        sent = sentiment_data.get(ticker, 0.5)
        metrics = get_all_metrics(vol, hist_vols, sent)
        results[ticker] = metrics
    return results

Reference: CFG-05, use st.cache_data(ttl=900) per locked decision
  </action>
<verify>grep -q "@st.cache_data" app.py && echo "Cached functions added"</verify>
  <done>app.py has cached data loading with TTL=900s</done>
</task>

<task type="auto">
  <name>Task 3: Build main dashboard UI with all visualizations</name>
  <files>app.py</files>
<action>
Update app.py main section to render complete dashboard:

1. Header section (keep existing):
   st.title("📈 AI Market Risk Early Warning System")
   st.markdown("### Real-time market monitoring with sentiment analysis")

2. Load data using cached functions:
   market_data, historical = load_market_data(selected_tickers, time_range)
   news, sentiment = load_news_and_sentiment(selected_tickers)
   risk_metrics = calculate_risk_metrics(market_data, sentiment, selected_tickers)

3. Extract aggregate values (use first ticker or average):
   avg_risk = sum(m['risk_score'] for m in risk_metrics.values()) / len(risk_metrics)
   avg_vol = sum(m['vol_zscore'] for m in risk_metrics.values()) / len(risk_metrics)
   avg_sent = sum(m['normalized_sentiment'] for m in risk_metrics.values()) / len(risk_metrics)

4. Render risk alert banner:
   from src.charts import create_risk_banner
   level, color, alert_type = get_risk_level(avg_risk)
   banner = create_risk_banner(level, alert_type)
   st.alert(banner['message'], icon=banner['icon'])

5. Render 3 gauges row:
   from src.charts import create_three_gauges
   gauges = create_three_gauges(avg_vol * 50, avg_sent * 100, avg_risk)
   col1, col2, col3 = st.columns(3)
   col1.plotly_chart(gauges[0], use_container_width=True)
   col2.plotly_chart(gauges[1], use_container_width=True)
   col3.plotly_chart(gauges[2], use_container_width=True)

6. Render candlestick chart (7-day SPY):
   if 'SPY' in historical:
       from src.charts import create_candlestick
       st.plotly_chart(create_candlestick(historical['SPY']), use_container_width=True)

7. Render sentiment trend line (if historical data available):
   Use mock 7-day dates and sentiment values for now

8. Render volatility heatmap:
   vol_data = {t: historical.get(t, pd.DataFrame())['Close'].pct_change().std() for t in selected_tickers}

9. Render details table:
   st.dataframe(pd.DataFrame.from_dict(risk_metrics, orient='index'))

Reference: UI-01 to UI-07 requirements
  </action>
<verify>python -c "
import sys
sys.path.insert(0, '.')
# Check app.py imports work (not full execution which needs data)
with open('app.py') as f:
    content = f.read()
    assert 'st_autorefresh' in content
    assert 'create_three_gauges' in content
    assert 'create_candlestick' in content
    assert 'st.cache_data' in content
print('Import checks passed')
"</verify>
  <done>Dashboard renders all UI components: gauges, banner, charts, table</done>
</task>

</tasks>

<verification>
1. Sidebar: Check ticker multiselect works, time range selection works
2. Auto-refresh: Verify 15-minute interval configured
3. Manual refresh: Verify button clears cache and reruns
4. Data loading: Verify market data, news, sentiment all load with caching
5. UI rendering: Verify all charts display correctly

Final verification: Run `streamlit run app.py` and check:
- Dashboard loads without errors
- 3 gauges display in top row
- Alert banner shows with correct color
- Candlestick chart renders for SPY
- Heatmap and table display
</verification>

<success_criteria>
- [ ] Sidebar has ticker selection (add/remove)
- [ ] Sidebar has time range selector (1d/7d/30d)
- [ ] Auto-refresh triggers every 15 minutes
- [ ] Manual refresh button works
- [ ] Data cached with TTL=900s
- [ ] 3 gauges display (volatility, sentiment, risk)
- [ ] Risk alert banner displays with correct color
- [ ] Candlestick chart renders for SPY
- [ ] Volatility heatmap renders
- [ ] Details table shows per-ticker breakdown
</success_criteria>

<output>
After completion, create `.planning/phases/03-risk-dashboard/03-03-SUMMARY.md`
</output>
