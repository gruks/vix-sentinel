---
phase: 01-data-pipeline
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - app.py
  - src/__init__.py
  - src/data_fetcher.py
  - src/news_fetcher.py
  - src/cache.py
autonomous: true

must_haves:
  truths:
    - "User can fetch live SPY and VIX prices via yfinance"
    - "Rolling volatility is calculated from market returns"
    - "Google News RSS feeds parse correctly for each ticker"
    - "TechCrunch RSS feed provides tech news headlines"
    - "Hacker News API returns top stories"
    - "Data is cached for 1 hour to avoid rate limits"
  artifacts:
    - path: "requirements.txt"
      provides: "Python dependencies"
    - path: "app.py"
      provides: "Main Streamlit entry point"
    - path: "src/data_fetcher.py"
      provides: "Market data fetching with yfinance"
      exports: ["fetch_market_data", "calculate_volatility"]
    - path: "src/news_fetcher.py"
      provides: "News aggregation from RSS + HN API"
      exports: ["fetch_google_news", "fetch_techcrunch", "fetch_hacker_news", "fetch_all_news"]
    - path: "src/cache.py"
      provides: "Data caching layer"
      exports: ["get_cached", "set_cached"]
  key_links:
    - from: "app.py"
      to: "src/data_fetcher.py"
      via: "import and function call"
      pattern: "from src.data_fetcher import"
    - from: "app.py"
      to: "src/news_fetcher.py"
      via: "import and function call"
      pattern: "from src.news_fetcher import"
    - from: "src/data_fetcher.py"
      to: "src/cache.py"
      via: "cache decorator or function"
      pattern: "from src.cache import"
---

<objective>
Create data pipeline foundation - fetch and aggregate market data (SPY, VIX) and news from multiple sources (Google News RSS, TechCrunch RSS, Hacker News API).

Purpose: This is the data layer that feeds sentiment analysis (Phase 2) and risk calculation (Phase 3).
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-data-pipeline/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up project structure</name>
  <files>requirements.txt, app.py, src/__init__.py</files>
  <action>
Create project structure:

1. requirements.txt with dependencies:
- yfinance>=0.2.0
- pandas>=2.1.0
- feedparser>=6.0.10
- requests>=2.31.0
- streamlit>=1.28.0
- plotly>=5.17.0

2. src/__init__.py - empty package marker

3. app.py - basic Streamlit app with:
- st.set_page_config with wide layout
- Header: "AI Market Risk Early Warning System"
- Import statements for data_fetcher and news_fetcher
- Placeholder for future dashboard
- Auto-refresh every 15 minutes using @st.fragment or meta refresh
  </action>
  <verify>
Run: pip install -r requirements.txt && streamlit run app.py --server.headless true
Expected: App starts without import errors
  </verify>
  <done>
Project structure exists, requirements.txt created, app.py starts without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement market data fetcher</name>
  <files>src/data_fetcher.py</files>
  <action>
Create src/data_fetcher.py with:

1. fetch_market_data(tickers: list) -> dict:
   - Use yfinance to fetch 1d data for ['SPY', 'VIX'] + tickers
   - Return: {ticker: {price, change_pct, volume, history_df}}

2. calculate_volatility(prices: pd.DataFrame, period: int = 20) -> float:
   - Calculate rolling standard deviation of returns
   - Return volatility as annualized percentage
   - Handle edge cases (insufficient data)

3. Use @st.cache_data(ttl=900) decorator for caching
   - Cache market data for 15 minutes

4. Error handling:
   - Try/except around yfinance calls
   - Return None values on failure instead of crashing
   - Log errors for debugging
  </action>
  <verify>
Test in Python:
```python
from src.data_fetcher import fetch_market_data, calculate_volatility
data = fetch_market_data(['AAPL', 'TSLA'])
print(data['SPY']['price'])  # Should show price
```
  </verify>
  <done>
fetch_market_data returns live prices, calculate_volatility computes rolling volatility
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement news aggregator</name>
  <files>src/news_fetcher.py</files>
  <action>
Create src/news_fetcher.py with:

1. fetch_google_news(ticker: str, limit: int = 5) -> list:
   - Parse Google News RSS: https://news.google.com/rss/search?q={ticker}+stock
   - Extract: title, source, published date
   - Respect rate limits: 1s delay between requests

2. fetch_techcrunch(limit: int = 3) -> list:
   - Parse TechCrunch RSS: https://techcrunch.com/feed/
   - Extract: title, source, published date

3. fetch_hacker_news(limit: int = 5) -> list:
   - GET https://hacker-news.firebaseio.com/v0/topstories.json
   - Get top N story IDs, then fetch each story detail
   - Extract: title, source (Hacker News)

4. fetch_all_news(tickers: list) -> dict:
   - Aggregate all sources
   - Return: {headlines: [...], sources: {...}}

5. Use @st.cache_data(ttl=3600) - cache for 1 hour
   - Cache news to avoid rate limiting

6. Error handling:
   - Try/except for each feed
   - Return empty list on failure
   - Log errors
  </action>
  <verify>
Test in Python:
```python
from src.news_fetcher import fetch_all_news
news = fetch_all_news(['AAPL', 'MSFT'])
print(len(news['headlines']))  # Should have headlines
```
  </verify>
  <done>
All three news sources return aggregated headlines
  </done>
</task>

<task type="auto">
  <name>Task 4: Implement caching layer</name>
  <files>src/cache.py</files>
  <action>
Create src/cache.py with:

1. Simple pickle-based cache for persistent storage:
   - get_cached(key: str) -> any
   - set_cached(key: str, value: any, ttl: int = 3600)

2. Use .cache/ directory for pickle files

3. Cache invalidation based on TTL

4. Fallback: If cache read fails, return None (trigger fresh fetch)

This is a backup to Streamlit's built-in caching - useful for debugging and persistence.
  </action>
  <verify>
Test:
```python
from src.cache import get_cached, set_cached
set_cached("test", {"value": 123}, ttl=60)
assert get_cached("test") == {"value": 123}
```
  </verify>
  <done>
Cache layer functional for persistent data storage
  </done>
</task>

</tasks>

<verification>
Run app.py and verify:
1. Market data section shows SPY and VIX prices
2. News section displays headlines from all 3 sources
3. No errors in console
4. Data refreshes on manual refresh button
</verification>

<success_criteria>
- [x] requirements.txt created with all dependencies
- [x] app.py starts Streamlit without errors
- [x] fetch_market_data returns live SPY/VIX prices
- [x] calculate_volatility computes rolling volatility
- [x] Google News RSS parses correctly
- [x] TechCrunch RSS parses correctly
- [x] Hacker News API returns stories
- [x] News aggregated from all sources
- [x] Data cached for 1 hour
- [x] Error handling prevents crashes
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-01-SUMMARY.md`
</output>
