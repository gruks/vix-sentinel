---
phase: 04-alerts-deployment
plan: 03
type: execute
wave: 2
depends_on: [04-01, 04-02]
files_modified: [requirements.txt]
autonomous: true
user_setup:
  - service: streamlit-cloud
    why: "Deploy dashboard to Streamlit Community Cloud"
    env_vars: []
    dashboard_config:
      - task: "Connect GitHub repository"
        location: "https://share.streamlit.io/"
      - task: "Add email secrets"
        location: "App settings -> Advanced -> Secrets"
      - task: "Deploy app"
        location: "Click Deploy button"

must_haves:
  truths:
    - "requirements.txt contains all dependencies with versions"
    - "App can be deployed to Streamlit Cloud"
    - "Secrets can be configured in deployment"
  artifacts:
    - path: "requirements.txt"
      provides: "All Python dependencies with pinned versions"
      contains: ["streamlit", "yfinance", "plotly", "pandas", "requests", "feedparser", "streamlit-autorefresh"]
  key_links:
    - from: "requirements.txt"
      to: "app.py"
      via: "pip install -r requirements.txt"
      pattern: "dependencies.*pinned"
---

<objective>
Complete requirements.txt and prepare for deployment

Purpose: Complete DEPL-01 (requirements.txt), prepare deployment configuration (DEPL-03)
Output: Updated requirements.txt, deployment preparation
</objective>

<execution_context>
@C:/Users/HP/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/HP/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-alerts-deployment/04-RESEARCH.md

# Current project files
@requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update requirements.txt with all dependencies</name>
  <files>requirements.txt</files>
  <action>
Update requirements.txt to include ALL dependencies with appropriate versions:

1. Read current requirements.txt
2. Update/add the following dependencies:
   ```
   streamlit>=1.28.0
   yfinance>=0.2.0
   pandas>=2.1.0
   numpy>=1.24.0
   plotly>=5.17.0
   requests>=2.31.0
   feedparser>=6.0.10
   streamlit-autorefresh>=1.0.0
   transformers>=4.30.0
   torch>=2.0.0
   ```
   
   Note: transformers and torch are needed for FinBERT sentiment analysis

3. Pin versions for reproducibility (use >= for flexibility):
   - Keep existing packages with minimum version constraints
   - Add numpy (needed for calculations)
   - Add transformers (needed for FinBERT)
   - Add torch (needed for FinBERT)
  </action>
  <verify>
Check requirements.txt contains all required packages
Verify each import works: python -c "import streamlit, yfinance, pandas, numpy, plotly, requests, feedparser, transformers, torch"
  </verify>
  <done>
requirements.txt updated with:
- streamlit, yfinance, pandas, numpy, plotly
- requests, feedparser, streamlit-autorefresh
- transformers, torch (for FinBERT)
</done>
</task>

<task type="auto">
  <name>Task 2: Document deployment steps</name>
  <files>.planning/phases/04-alerts-deployment/DEPLOYMENT.md</files>
  <action>
Create a DEPLOYMENT.md file with instructions for Streamlit Cloud deployment:

1. Create .planning/phases/04-alerts-deployment/DEPLOYMENT.md with:

```
# Deployment Guide: VIX Sentinel to Streamlit Cloud

## Prerequisites
- GitHub repository with all code pushed
- Gmail account with 2FA enabled (for email alerts)

## Step 1: Prepare Secrets
1. Enable 2FA on your Gmail account
2. Generate an App Password:
   - Go to https://myaccount.google.com/apppasswords
   - Select "Mail" and "Other (Custom name)"
   - Copy the 16-character app password

## Step 2: Deploy to Streamlit Cloud
1. Go to https://share.streamlit.io/
2. Sign in with GitHub
3. Click "New app"
4. Select your repository, branch, and main file path (app.py)
5. Click "Deploy"

## Step 3: Configure Secrets (After Deployment)
1. In your deployed app page, click "Edit secrets" (gear icon)
2. Add the following:
   ```toml
   [smtp]
   email = "your-email@gmail.com"
   password = "your-app-password"
   alert_email = "recipient@example.com"
   ```
3. Click "Save"

## Step 4: Verify Deployment
- App should load with auto-refresh every 15 minutes
- Check sidebar shows "Enable email alerts" checkbox
- Enable alerts and verify configuration status

## Troubleshooting
- If emails fail: Check app password is correct (16 characters)
- If app won't load: Check requirements.txt has all dependencies
- Auto-refresh: Works automatically in cloud deployment
```

This documents the DEPL-03 requirement for user setup
  </action>
  <verify>
DEPLOYMENT.md created with step-by-step instructions
  </verify>
  <done>
DEPLOYMENT.md documents:
- Prerequisites (GitHub, Gmail with 2FA)
- App Password generation
- Streamlit Cloud deployment steps
- Secrets configuration
- Verification steps
</done>
</task>

</tasks>

<verification>
- requirements.txt has all dependencies with versions
- DEPLOYMENT.md created with clear instructions
- All dependencies can be imported
</verification>

<success_criteria>
- DEPL-01: requirements.txt complete with all dependencies
- DEPL-02: .env.example exists (from plan 04-01)
- DEPL-03: Deployment documented and ready to execute
- DEPL-04: Auto-refresh already works in existing app.py
</success_criteria>

<output>
After completion, create `.planning/phases/04-alerts-deployment/04-03-SUMMARY.md`
</output>
